<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Easy way to manage shared expenses">
    <meta name="author" content="Jason Dsouza">

    <title>Transactor</title>

    <!-- Le styles -->
    <%= stylesheet_link_tag    'application' %>

    <!-- Custom styles for this template -->
    <link href="jumbotron-narrow.css" rel="stylesheet">
  </head>

  <body>

    <div class="container-narrow">

      <div class="header">
        <ul class="nav nav-pills pull-right">
          <%= content_tag :li, :class => ('active' if current_page?(:controller => "home")) do %>
            <%= link_to "Home", home_path %>
          <% end %>
          <%= content_tag :li, :class => ('active' if current_page?(:controller => "transactions")) do %>
            <%= link_to "Transactions", transactions_path %>
          <% end %>
          <%= content_tag :li, :class => ('active' if current_page?(:controller => "labels")) do %>
            <%= link_to "Labels", labels_path %>
          <% end %>
          <%= content_tag :li, :class => ('active' if current_page?(:controller => "users")) do %>
            <%= link_to "Users", users_path %>
          <% end %>
        </ul>
        <h3 class="muted">Transactor</h3>
      </div>

      <%= yield %>

      <div class="footer">
        <p>&copy; Jason Dsouza</p>
      </div>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="../assets/js/jquery.js"></script>
    <script src="../assets/js/bootstrap.js"></script>

  </body>
</html>
